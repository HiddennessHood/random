var options = {
    width: 340,
    height: 192,
    channel: "honeymad",
    parent: ["hiddennesshood.github.io"]    
  };
  var player = new Twitch.Player("streamerplayer", options);
  player.setVolume(1);
